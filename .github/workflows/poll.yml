name: Poll aircraft position

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch: {}

jobs:
  poll:
    runs-on: ubuntu-latest
    steps:
      - name: Call /api/poll on the deployed site
        run: curl -sf "${{ secrets.SITE_URL }}/api/poll"
